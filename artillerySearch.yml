config:
  target: "http://localhost:3001"
  phases:
    - duration: 60
      arrivalRate: 40
  defaults:
    headers:
      x-my-service-auth: "987401838271002188298567"
scenarios:
  - flow:
      - loop:
          - get:
              url: "/"
        count: 200
